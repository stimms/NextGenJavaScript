// Generated by CoffeeScript 1.6.2
var BarChart, woodChuckChart,
  __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

BarChart = (function() {
  var data;

  data = [2, 4, 5, 9, 10, 3];

  function BarChart(name) {
    this.name = name;
  }

  BarChart.prototype.checkTitleLength = function() {
    if (this.name.length > 25) {
      return console.log("long name");
    } else {
      return console.log("short name");
    }
  };

  BarChart.prototype.firstElement = function() {
    return data[0];
  };

  BarChart.prototype.first3Elements = function() {
    return data.slice(0, 3);
  };

  BarChart.prototype.first2Elements = function() {
    return data.slice(0, 2);
  };

  BarChart.prototype.lastElement = function() {
    return data[data.length];
  };

  BarChart.prototype.listDataElements = function() {
    var d, i, _i, _len, _results;

    _results = [];
    for (i = _i = 0, _len = data.length; _i < _len; i = ++_i) {
      d = data[i];
      _results.push(console.log("Data element " + i + ": " + d));
    }
    return _results;
  };

  BarChart.prototype.fillDataRandomly = function() {
    var d, _i, _results;

    _results = [];
    for (d = _i = 0; _i <= 10; d = ++_i) {
      _results.push(data.push(Math.random()));
    }
    return _results;
  };

  BarChart.prototype.checkDataLength = function() {
    if (data.length === 6) {
      return console.log("Exactly 6");
    }
  };

  BarChart.prototype.dataContains = function(toCheck) {
    if (__indexOf.call(data, toCheck) >= 0) {
      return console.log("In there");
    } else {
      return console.log("not there");
    }
  };

  return BarChart;

})();

woodChuckChart = new BarChart("Quantity of Wood Chucked by Average Woodchuck");

woodChuckChart.checkTitleLength();

console.log(woodChuckChart.first2Elements());

woodChuckChart.listDataElements();

woodChuckChart.checkDataLength();

woodChuckChart.fillDataRandomly();

woodChuckChart.listDataElements();

woodChuckChart.dataContains(2);
